# Web server
flask>=3.0.0
flask-limiter>=3.5.0
gunicorn>=21.0.0

# Scraping — fully keyless, no API accounts needed
duckduckgo-search>=6.2.0
requests>=2.31.0
beautifulsoup4>=4.12.0

# Data
pandas>=2.0.0
numpy>=1.24.0

# NLP / Sentiment - CPU-only torch for faster builds
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0
sentencepiece>=0.1.99

# Image processing - headless = no GUI deps, smaller build
opencv-python-headless>=4.8.0
scikit-image>=0.21.0
Pillow>=10.0.0
scipy>=1.11.0

# VLM / LLM via OpenRouter (OpenAI-compatible client)
openai>=1.30.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.13.0

# Metrics
scikit-learn>=1.3.0
</parameter>
<task_progress>
- [x] Optimize requirements.txt for CPU-only torch
- [ ] Add lazy imports to reduce startup time
- [ ] Parallelize scraper country processing
- [ ] Parallelize image downloads
- [ ] Batch VLM/LLM API calls
- [ ] Add CSV caching to Flask app
</task_progress>
</invoke>