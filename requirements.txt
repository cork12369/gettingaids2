# Web server
flask>=3.0.0
flask-limiter>=3.5.0
gunicorn>=21.0.0

# Scraping
praw>=7.7.0
flickrapi>=2.4.0
requests>=2.31.0
beautifulsoup4>=4.12.0

# Data
pandas>=2.0.0
numpy>=1.24.0

# NLP / Sentiment
transformers>=4.35.0
torch>=2.0.0          # CPU-only is fine, ~800MB — see note below
sentencepiece>=0.1.99

# Image processing
opencv-python-headless>=4.8.0  # headless = no GUI deps, smaller image
scikit-image>=0.21.0
Pillow>=10.0.0
scipy>=1.11.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.13.2

# NOTE: torch CPU-only is ~800MB installed.
# If Zeabur build times out, replace with:
#   torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu
# and add that index URL to a pip.conf or pass via zbpack config.
